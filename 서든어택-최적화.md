# 서든어택 최적화

윈도우 설정을 서든어택에 맞게 **버튼 한 번**으로 바꿉니다.
그리고 바꾼 값을 전부 적어두기 때문에 **언제든 원래대로 되돌릴 수 있습니다.**

> 게임 파일은 건드리지 않습니다. 윈도우 설정만 바꿉니다.

---

## 0. 쓰는 법 — 더블클릭 두 개

| 무엇 | 파일 |
|---|---|
| **최적화하기** | **`서든어택최적화.bat`** ⬅ 이걸 더블클릭 |
| **원래대로 되돌리기** | **`최적화되돌리기.bat`** |

윈도우 전용입니다. 서든어택이 윈도우에서만 도는 게임이라 그렇습니다.

더블클릭하면 이렇게 진행됩니다.

1. **"이 앱이 디바이스를 변경하도록 허용하시겠어요?"** → **예**
   (전원 계획·네트워크처럼 컴퓨터 전체에 걸리는 항목은 관리자 권한이 있어야 합니다.
   **아니오**를 눌러도 프로그램은 뜹니다. 대신 그 항목들이 잠긴 채로 보입니다.)
2. 브라우저에 화면이 뜹니다 (`http://127.0.0.1:8770`)
3. 맨 위 **파란 버튼 하나**를 누릅니다
4. 끝. 몇 초 걸립니다.

파이썬이 없으면 설치 페이지를 대신 열어줍니다.
설치할 때 화면 아래 **"Add Python to PATH"** 에 체크하는 것만 잊지 마세요.

---

## 1. 화면에서 보이는 것

**맨 위 — 내 컴퓨터**
CPU·그래픽카드·메모리·윈도우 버전, 그리고 **모니터가 지금 몇 Hz 로 돌고 있는지**.
`1920×1080 · 60Hz → 최대 144Hz 가능` 처럼 나오면 지금 손해 보고 있다는 뜻입니다.

**가운데 — 한 번에 최적화**
권장 항목 중 아직 안 된 것만 골라서 적용합니다. 이미 되어 있는 건 건드리지 않습니다.

**아래 — 항목 목록**
항목마다 지금 상태(`적용됨` / `안 됨`)와 **왜 바꾸는지**가 한 줄씩 적혀 있습니다.
체크를 풀면 그 항목은 안 건드립니다. `고른 것만 적용` 으로 원하는 것만 할 수도 있습니다.

**그 아래 — 되돌리기**
마지막으로 언제 무엇을 바꿨는지와 **원래대로 되돌리기** 버튼.

**맨 아래 — 직접 하셔야 하는 것**
게임 안 옵션과 그래픽카드 제어판 설정. 이건 자동으로 못 바꿔서 안내만 합니다.

---

## 2. 무엇을 바꾸나

### 기본으로 켜져 있는 것 (권장)

| 항목 | 무엇을 | 왜 |
|---|---|---|
| **마우스 가속 끄기** | 포인터 정확도 향상 해제 | 빨리 움직이면 더 많이 돌던 게 없어집니다. **에임에 제일 큽니다** |
| **모니터 주사율 최대로** | 지금 해상도에서 낼 수 있는 최대 Hz | 144Hz 모니터를 60Hz 로 쓰면 초당 60장만 보입니다 |
| **전체 화면 최적화 끄기** | 서든어택 실행 파일에 설정 | 윈도우가 게임을 몰래 창 모드로 돌리는 걸 막습니다 |
| **윈도우 화면 효과 줄이기** | 창 애니메이션·메뉴 지연 해제 | 프레임보다는 알트탭이 빨라집니다 |
| **게임용 전원 계획** | 새 계획을 만들어 켜기 | CPU가 절전하려고 속도를 낮추지 않게 |
| **네트워크 지연 줄이기** | Nagle 알고리즘 끄기 | 신호를 모았다 보내는 시간이 그대로 핑이 됩니다 |
| **네트워크 속도 제한 풀기** | NetworkThrottlingIndex 해제 | 윈도우가 걸어둔 초당 패킷 제한을 풉니다 |
| **게임에 CPU·GPU 우선 배정** | 멀티미디어 스케줄러 '게임' 등급 조정 | 배경 프로그램보다 게임을 먼저 처리 |
| **윈도우 게임 모드 켜기** | 게임 모드 활성화 | 게임 중 업데이트·배경 작업을 미룹니다 |
| **배경 녹화(Game DVR) 끄기** | Xbox Game Bar 녹화 해제 | 늘 몰래 녹화하느라 프레임을 깎아먹습니다 |
| **서든어택 우선순위 '높음'** | 실행 시 CPU 우선순위 | 작업 관리자에서 매번 손으로 바꾸던 값입니다 |

### 직접 켜야 하는 것 (기본 꺼짐)

| 항목 | 왜 기본이 아닌가 |
|---|---|
| **알림 팝업 끄기** | 게임이 아닐 때도 알림이 안 옵니다 |
| **하드웨어 가속 GPU 일정 예약 끄기** | 재부팅이 필요하고, 컴퓨터마다 결과가 다릅니다 |
| **백신 실시간 검사에서 게임 폴더 빼기** | 그 폴더는 검사를 안 하게 됩니다. 공식 경로로 설치한 게임에만 쓰세요 |

---

## 3. 일부러 **안** 하는 것

다른 최적화 프로그램들이 흔히 손대는 것들입니다. 빠뜨린 게 아니라, 얻는 것보다 잃는 게
커서 뺐습니다.

| 안 하는 것 | 이유 |
|---|---|
| `bcdedit` — HPET · 플랫폼 클럭 | 잘못되면 **윈도우가 아예 안 켜집니다.** 프레임이 오른다는 근거도 확실하지 않습니다 |
| 가상 메모리(페이지 파일) 끄기 | 메모리가 모자라는 순간 게임이 경고 없이 꺼집니다 |
| 윈도우 서비스 대량 정지 | 소리가 안 나거나 업데이트가 멈추는 식으로 한참 뒤에 조용히 고장 납니다 |
| 레지스트리 청소 · 임시파일 삭제 | 프레임과 관계가 없고, 지운 건 되돌릴 수도 없습니다 |
| 게임 파일 수정 | 게임을 바꾸는 일이라 계정이 막힐 수 있습니다 |

---

## 4. 되돌리기

**바꾸기 전 값을 그대로 다시 넣습니다.** 원래 없던 값은 지웁니다.
"기본값으로 초기화" 가 아니라 **당신 컴퓨터가 원래 갖고 있던 값**으로 돌아갑니다.

| 방법 | |
|---|---|
| 화면에서 | **원래대로 되돌리기** 버튼 |
| 화면 없이 | **`최적화되돌리기.bat`** 더블클릭 |
| 터미널에서 | `python sudden.py revert` |

기록은 `backup/최적화기록-날짜-시각.json` 에 쌓입니다. 항목 하나를 바꿀 때마다
파일에 먼저 적기 때문에, 중간에 전원이 나가도 거기까지는 되돌릴 수 있습니다.

전원 계획은 **쓰던 계획을 고치지 않고 복제해서** 새로 만듭니다. 되돌리기는 원래 계획으로
돌아간 뒤 우리가 만든 걸 지우는 것이라, 손수 맞춰둔 설정이 사라지지 않습니다.

---

## 5. 서든어택을 못 찾았다고 나오면

세 항목(전체 화면 최적화·우선순위·검사 제외)은 실행 파일 위치를 알아야 합니다.
넥슨 런처는 설치 폴더를 사용자가 정할 수 있어서 한 곳에 박혀 있지 않습니다.

화면 위쪽 칸에 **설치 폴더나 `.exe` 경로**를 넣고 `찾기` 를 누르면 됩니다.
(예: `C:\Nexon\SuddenAttack`)

찾는 방법: 바탕화면 서든어택 바로가기 **우클릭 → 파일 위치 열기**.

나머지 항목들은 게임 경로를 몰라도 전부 적용됩니다.

---

## 6. 터미널에서 쓰기

```bash
python sudden.py            # 화면 띄우기 (기본)
python sudden.py status     # 지금 상태만 출력
python sudden.py apply      # 권장 항목 바로 적용
python sudden.py apply --only mouse_accel refresh_rate
python sudden.py revert     # 마지막 최적화 되돌리기
```

---

## 7. 자주 나오는 상황

**Q. 프레임이 그대로인데요**
이 프로그램은 윈도우 쪽 병목만 치웁니다. 프레임 자체는 대개 **게임 안 옵션**과
**그래픽카드 제어판**에서 갈립니다. 화면 맨 아래 `직접 하셔야 하는 것` 을 펼쳐보세요.
특히 **수직 동기 끄기**와 **그림자·효과 낮추기** 가 큽니다.

**Q. 화면이 안 뜹니다**
브라우저 주소창에 `http://127.0.0.1:8770` 을 직접 넣어보세요.
포트가 쓰이는 중이면 8771, 8772… 로 옮겨 뜹니다. 검은 창에 실제 주소가 적혀 있습니다.

**Q. 항목이 잠겨 있습니다 (`관리자 권한이 필요합니다`)**
`서든어택최적화.bat` 을 **우클릭 → 관리자 권한으로 실행** 하세요.

**Q. 되돌렸는데 화면이 그대로예요**
`윈도우 화면 효과`와 `하드웨어 가속 GPU 일정 예약` 은 다시 로그인하거나 재부팅해야
완전히 반영됩니다. 마우스·주사율·전원은 바로 반영됩니다.

**Q. 이거 계정 밴 당하지 않나요**
게임 파일이나 게임 메모리는 전혀 건드리지 않습니다. 윈도우 제어판에서 손으로 바꿀 수
있는 설정들을 대신 눌러주는 것뿐입니다.

---

## 8. 프로그램 구조 (고칠 분만)

| 파일 | 하는 일 |
|---|---|
| `sudden.py` | 시작 지점 (화면 / status / apply / revert) |
| `sudden_attack/tweaks.py` | **최적화 항목 전부.** 항목을 더하고 빼는 곳 |
| `sudden_attack/engine.py` | 적용·되돌리기 실행, 무엇이 막혔는지 판단 |
| `sudden_attack/backup.py` | 되돌리기 기록 저장·읽기 |
| `sudden_attack/winreg_io.py` | 레지스트리 읽기·쓰기 (+ 검사용 가짜 저장소) |
| `sudden_attack/shell.py` | powercfg·PowerShell 실행, 관리자 권한 확인 |
| `sudden_attack/display.py` | 모니터 주사율 조회·변경 |
| `sudden_attack/hardware.py` | CPU·그래픽·메모리·윈도우 정보 |
| `sudden_attack/game.py` | 서든어택 설치 위치 찾기 |
| `sudden_attack/guide.py` | 자동으로 못 바꾸는 것 안내 |
| `sudden_attack/ui.py` | 브라우저 화면 |

윈도우 API를 쓰는 부분은 전부 **가짜로 바꿔 끼울 수 있게** 되어 있어서, 윈도우가 없어도
`python -m pytest tests/test_sudden_*.py` 로 "적용 → 되돌리기가 정말 원래 값으로
돌아오는가" 를 검사할 수 있습니다.

**항목을 추가하려면** `tweaks.py` 의 `catalog()` 에 `Tweak(...)` 을 하나 더 넣으면 됩니다.
레지스트리 값이면 `RegistryAction([RegItem(...)])` 만 쓰면 되돌리기는 저절로 됩니다.
